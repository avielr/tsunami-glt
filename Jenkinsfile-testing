properties([
buildDiscarder(
    logRotator(numToKeepStr: '50')),
    parameters([
    string(
        defaultValue: '\'192.168.1.2\',\'10.0.0.50\',\'192.168.1.3\',\'172.31.1.1\'', 
        description: '''Enter URL / IP to scan in the following format \'\',\'\',\'\'''', 
        name: 'serversToScan', trim: true
    ), 
    string(
        defaultValue: 'test@test.com', 
        description: '', 
        name: 'E-Mail address', 
        trim: true
    )
    ]), 
    pipelineTriggers([
    upstream('1.Build-tsunami')
    ])
])



def stepsForParallel = serversToScan.split(',')collectEntries {
 ["${it}" : transformIntoStep(it)]
}

parallel stepsForParallel

def transformIntoStep(inputString) {
  return {
    podTemplate(yaml: """
    apiVersion: v1
    kind: Pod
    spec:
      containers:
      - name: tsunami
        image: aviel1988/tsunami-glt
        imagePullPolicy: IfNotPresent
        command:
        - cat
        tty: true
        volumeMounts:
        - mountPath: '/workspace'
          name: sharedworkspace
      - name: busybox
        image: busybox
        imagePullPolicy: IfNotPresent
        command: ["cat"]
        tty: true
        volumeMounts:
        - mountPath: '/workspace'
          name: sharedworkspace
      volumes:
      - name: sharedworkspace
        emptyDir: {}
    """
    ){
      node(POD_LABEL) {
        stage("Scan"){
            env.inputString = inputString
            if(inputString.matches(".*[a-z].*")){
                echo "inputString is a url ${inputString}"
                env.tsunamiCLI = sh(returnStdout: true, script: 'echo --hostname-target=')
            } else {
                echo "inputString is an IP ${inputString}" 
                env.tsunamiCLI = sh(returnStdout: true, script: 'echo --ip-v4-target=')
            }
            container('tsunami'){
                env.testString = sh(returnStdout: true, script: 'echo "${inputString}"')
                env.testString = env.testString.replace("'", "")
                env.testTsunamiCLI = sh(returnStdout: true, script: 'echo "${tsunamiCLI}"')
                string filename = "/workspace/tsunami-output.json"
                timeout(time: 2700, unit: 'SECONDS'){ // Full Tsunami scan took 36.82 min.
                RUN_CMD = testTsunamiCLI.trim() + testString.trim()
                //echo "RUN_CMD=${RUN_CMD}"
                sh "cd /usr/tsunami; java -cp \"tsunami.jar:plugins/nmap_port_scanner-0.0.1-SNAPSHOT.jar\" -Dtsunami.config.location=/usr/tsunami/tsunami.yaml com.google.tsunami.main.cli.TsunamiCli ${RUN_CMD} --scan-results-local-output-format=JSON --scan-results-local-output-filename=/workspace/tsunami-output.json"
                }
            }
        }

        stage("Report"){
          container('busybox'){
            sh 'cat /workspace/tsunami-output.json'
          }
        }
      }
    }
  }
}
